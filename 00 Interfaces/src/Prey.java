public interface Prey {

	// This interface does not use the default modifier, so every class that uses this interface
	//  must have this 'flee()' method.
	//  But note that since this does not have a body, every class that
	void flee();
}
